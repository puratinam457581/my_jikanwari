# 大学時間割管理PWA 実装仕様書

## 0. このドキュメントの使い方(Claude Codeへ)

あなたはこのアプリケーションの実装を担当するエンジニアです。以下の点を厳守してください。

- 本仕様書に記載のない挙動や、記載内容に曖昧さ・矛盾がある場合は、**推測で実装を進めず、必ずユーザーに質問してください**。誤った推測に基づく実装は手戻りコストが大きいため、確認コストの方を優先してください。
- ユーザーはプログラミング・アプリ開発の初学者です。技術的な選択を提案する際は、理由を添えて分かりやすく説明してください。
- 実装は本仕様書の「11. 実装フェーズ」に定義した順序で、フェーズ単位に区切って進めてください。1フェーズ完了ごとに動作確認を挟み、次フェーズに進む前にユーザーの確認を取ってください。
- コミットやファイル変更は小さい単位で行い、大規模な一括変更は避けてください。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| プロダクト名 | (未定。開発中の仮称は「時間割管理アプリ」とする) |
| 目的 | 大学の週間時間割・出欠・課題・単位取得状況を一元管理するPWA |
| 主要ユーザー | 開発者本人(大学1年生、Kitasato大学)。マルチユーザー機能は不要 |
| 動作環境 | iPhone Safari(ホーム画面に追加してPWAとして起動する運用を前提とする) |
| 予算制約 | **完全無料で構築すること**。有料API・有料クラウドサービス・課金が発生し得る構成は一切採用しないこと |
| 複数端末同期 | 不要。単一端末(ユーザーのiPhone)での完結を前提とする |

---

## 2. 技術スタック(確定事項)

| レイヤー | 採用技術 | 理由 |
|---|---|---|
| フロントエンド | React (関数コンポーネント + Hooks) | コマ単位の繰り返しUIとの相性、コンポーネント再利用性 |
| スタイリング | Tailwind CSS | 追加費用なし、Claude Codeとの相性 |
| ビルドツール | Vite | 高速な開発サーバー、PWAプラグインとの親和性 |
| PWA化 | vite-plugin-pwa | Service Worker / manifest.json の自動生成 |
| データ永続化 | IndexedDB(ラッパーとして `idb` ライブラリの使用を推奨) | サーバー不要・無料・オフライン動作 |
| バックアップ | JSONエクスポート/インポート機能を自前実装(後述 8.6) | 機種変更時のデータ移行手段として、クラウド同期の代替 |
| ホスティング | GitHub Pages または Cloudflare Pages(無料枠) | 静的サイトとして無料公開可能 |
| 通知 | Web Push API + Service Worker(ローカル通知ベース) | iOS 16.4以降、ホーム画面追加時のみ動作する制約を前提とする |

**禁止事項:** Firebase等の課金可能性があるクラウドサービス、有料API、サーバーサイド実装(バックエンドサーバーを立てる構成)は採用しないこと。

---

## 3. 画面構成(情報アーキテクチャ)

アプリ全体は下部タブバーによる4画面構成とする。

```
┌─────────────────────────────┐
│         (画面コンテンツ)         │
├─────┬─────┬─────┬─────┤
│ 時間割 │スケジュール│講義リスト│マイページ│
└─────┴─────┴─────┴─────┘
```

| タブ | 画面名 | 概要 |
|---|---|---|
| 1 | 時間割(ホーム) | 週間時間割グリッド表示。デフォルトタブ |
| 2 | スケジュール | 課題・締切の一覧(ToDo管理) |
| 3 | 講義リスト | 登録済み講義の一覧・新規追加・編集(4.2参照) |
| 4 | マイページ | 単位集計、設定(時限数・学期・卒業要件単位数等)への導線 |

上記に加え、以下の画面を実装する。

| 画面 | 遷移元 | 概要 |
|---|---|---|
| 授業詳細画面 | 時間割のコマタップ / 講義リストの項目タップ | 授業ごとの詳細・出欠管理・メモ・関連スケジュール |
| 講義編集フォーム | 講義リストの「新規追加」「編集」 | 講義の全属性を入力するフォーム |
| 出欠登録画面(モーダル) | 授業詳細画面の「追加」ボタン | 種別・日付を指定して出欠を1件記録 |
| スケジュール登録フォーム | スケジュール画面の「+」ボタン | 課題・締切を1件登録 |
| 設定画面(時限) | マイページ | 時限数・各時限の開始/終了時刻・表示曜日の設定 |
| 学期切替画面 | 時間割画面の「学期切替」ボタン | 前期/後期の切り替え、過去学期の閲覧 |
| 単位設定・進捗画面 | マイページ | 卒業/進級に必要な単位数の設定、現在の取得単位との比較表示 |


---

## 4. 画面詳細仕様

### 4.1 時間割画面(メイン)

**参照資料:** ユーザーから提供された既存アプリのスクリーンショット(会話内で共有済み。以下はその文字起こし仕様)。

**レイアウト構造:**

- ヘッダー: 中央に「{年}年 {学期名}」を表示(例:「2026年 前期」)。右上に「学期切替」ボタン(角丸のアウトラインピル型)を配置。学期名は「前期」「後期」の固定2種(4.9参照)。
- 曜日ヘッダー行: ユーザー設定に応じた曜日を横方向に表示(デフォルトは月〜土、日曜非表示。土曜非表示等のカスタマイズは 4.8 参照)。今日に該当する曜日は円形の色付きバッジで強調表示する。
- 時限グリッド: 縦方向に時限を並べる。各行の左端に時限番号を大きく表示し、その下に開始・終了時刻を「開始 - 終了」の2段書きで小さく表示する(例:「1」「09:00」「10:40」)。時限数・時刻はユーザー設定に従う(4.8参照)。
- コマ:
  - 講義が配置されているコマは、その講義に設定されたカラー(4.6参照)で背景を塗りつぶす。
  - コマ内上部に講義名を表示する(中央揃え、長い場合は自動折り返し)。
  - コマ内下部に教室名を表示するピル型バッジを表示する。教室名が未設定の場合は「未登録」とグレー文字で表示する(プレースホルダー)。
  - 空きコマは白背景とし、タップ可能な領域として機能させる。
- 今日・現在時限のハイライト: 今日の曜日列全体、および現在時刻が含まれる時限の行(または該当コマ)に、視覚的な強調(枠線または背景の差分)を付与する。強調の具体的な色・スタイルはTailwindのデフォルトから妥当なものを選定してよいが、既存コマの色と混同しない色調を選ぶこと。

**インタラクション:**

- 空きコマをタップ → 「この曜日・時限に配置する講義を選択する」モーダルまたは画面に遷移する。既存の講義リストから選択して配置するか、その場で新規講義を作成して配置するかをユーザーが選べるようにすること。
- 講義が配置済みのコマをタップ → 授業詳細画面(4.3)へ遷移する。
- 1コマにつき講義は1件のみ配置可能とする(4.7参照)。

### 4.2 講義リスト画面

**参照資料:** 具体的な参考UIなし。以下の要件を満たす範囲でレイアウトは実装者(Claude Code)の裁量とする。ただし、以下は必須要件とする。

- 登録済み講義を一覧表示する(カード形式・リスト形式いずれでもよい)。
- 各項目に、講義名・カラー(色見本)・曜日/時限・教室名を最低限表示する。
- 一覧の並び順は「曜日・時限順」をデフォルトとする。
- 新規講義追加ボタンを画面内に配置する(フローティングアクションボタン推奨、4.1のスケジュール画面と統一感を持たせること)。
- 各項目をタップすると、講義編集フォームまたは授業詳細画面に遷移する(遷移先はClaude Codeの判断でよいが、一貫性を持たせること)。

### 4.3 授業詳細画面

**参照資料:** ユーザー提供のスクリーンショットに準拠。

**レイアウト構造:**

- ヘッダー: 左に戻る矢印、中央に講義名、右上に「コマから外す」テキストリンク(このコマへの配置のみ解除し、講義データ自体は講義リストに残す操作)。
- 基本情報カード: 左端に講義カラーを示す縦長のカラーバー。教員名(アイコン付き)、教室名(アイコン付き)を表示。右側に編集(鉛筆)アイコンを配置し、タップで講義編集フォームへ遷移する。
- 出欠管理カード: 見出し「出欠管理」+ 右上「追加」ボタン(出欠登録モーダルを開く、4.4参照)。カード内に出席/欠席の2項目を、大きな数字とラベルで横並び表示する。出席不要な講義(4.5参照)の場合は、その旨をカード内に明示し、カウント表示ではなく「この授業は出席管理の対象外です」等のメッセージを表示する。
- スケジュールカード: 見出し「スケジュール」+ 右上「追加」ボタン(この講義に紐づくスケジュール登録フォームを開く)。この講義に関連付けられたスケジュール項目(締切日・カテゴリー)を一覧表示する。
- メモカード: 見出し「メモ」+ 右上「追加」ボタン。テキストメモを保存・表示する。未登録時はプレースホルダー文言を表示する。

### 4.4 出欠登録モーダル

- 出欠種別の選択: 出席 / 欠席 の2択(単一選択)。遅刻・早退は個別カウントの対象としない。
- 対象日付の指定: 日付ピッカーで指定(例:2026年8月24日)。
- 登録すると、該当講義・該当種別のカウントが1増加する。
- 同一講義・同一日付への複数回登録を許可するかは、Claude Code側で判断してよいが、誤操作防止のため「既に同日の記録があります。上書きしますか?」等の確認を入れることを推奨する。

### 4.5 出欠ルール設定(講義編集フォーム内)

- 各講義ごとに以下を設定可能にする:
  - 「出席管理の対象とするか」のON/OFFスイッチ(OFFの場合、授業詳細画面の出欠管理カードは前述の通り対象外表示にする)。
  - 欠席上限回数(数値入力。単位を落とすリスクの目安となる回数)。
- 出欠は出席・欠席の2種のみをカウントする(遅刻・早退は個別管理の対象外)。
- 欠席上限に対する警告表示: 現在の欠席数が上限に**到達した時点**でのみ、時間割画面の該当コマ、および授業詳細画面に赤色の警告表示を行う。段階的な閾値(上限の80%到達時など)による事前警告は実装しない。

### 4.6 カラー設定

- 講義編集フォーム内にカラー選択UIを設ける。
- プリセットカラー(6〜8色程度、パステル調)をボタンとして並べ、タップで選択できるようにする。
- 加えて「カスタム」を選択すると、HTML標準の `<input type="color">` を用いたカラーピッカーを表示し、任意のRGB(HEX)値を選択できるようにする。
- 選択したカラーはHEXコード文字列としてデータに保存する(データモデルは7章参照)。

### 4.7 同一コマへの複数講義配置(実装しない)

- 時間割の1コマ(曜日×時限)には、講義を1件のみ配置可能とする。複数講義の同時配置には対応しない。

### 4.8 時限・曜日のカスタマイズ(設定画面)

- 時限数: ユーザーが増減可能な数値設定(デフォルト6時限)。
- 各時限の開始・終了時刻: 時限ごとに個別設定可能。
- 表示曜日: 月〜日の7曜日について、それぞれ時間割に表示するかどうかをON/OFFで設定可能(デフォルトは月〜土のONと日曜のOFF)。
- 上記の設定変更は、既存の登録済み講義データに影響を与えないこと(例:時限数を減らして既存の講義が参照していた時限が消えた場合の扱いは、データを削除せず「非表示だが保持」とし、ユーザーに警告を表示すること)。

### 4.9 学期切替

- 前期・後期の2学期制のみ実装する(クォーター制は対象外)。
- 学期を切り替えても、過去の学期の時間割・出欠記録は削除せず保持し、いつでも参照可能にする(学期切替画面から過去学期を選択して閲覧するイメージ)。
- 学期ごとに独立した時間割データ(講義配置)を持つ。講義マスタ(講義リストの各講義情報)自体も学期ごとに独立させる。すなわち、学期が切り替わったら、たとえ同名・同内容の講義であっても講義リストへの新規登録が必要となる仕様とする(確定事項)。

### 4.10 スケジュール(課題管理)画面

**参照資料:** ユーザー提供のスクリーンショットに準拠。

- 日付ごとにグルーピングしたリスト表示(見出しは月単位、例:「2026年8月」)。
- 各予定カードに、日付バッジ(円形、日付+曜日)、時刻、タイトル、紐づく講義名(タグアイコン付き)を表示する。
- 各予定は完了/未完了のチェック状態を持つ(ToDo形式)。完了した予定の表示方法(取り消し線、リストから非表示にする等)はClaude Code側の裁量でよいが、完了済みを再度確認できる導線(フィルタ等)は用意すること。
- 画面右下にフローティングアクションボタン(「+」)を配置し、新規登録フォームを開く。
- 登録フォームの入力項目:
  - 関連する講義(講義リストから選択。任意項目とし、講義に紐づかないスケジュールも登録可能にする)
  - 締切日時(日付 + 時間)
  - カテゴリー(課題・レポート・テスト・休講・補講 の5種から選択する形とする。今後カテゴリーが追加される可能性を考慮し、選択肢はコード上で拡張しやすい形で実装すること)
  - メモ(自由記述)
  - 通知タイミング(1日前 / 3日前 / 通知なし の3択。複数選択可否は**ユーザーへの確認が必要**。デフォルトは単一選択とする)

### 4.11 単位集計・進捗画面(マイページ内)

- ユーザーが「卒業/進級に必要な単位数」を任意の数値として設定できる入力欄を設ける。
- 各講義に「単位取得済み」チェックを設け、チェックした講義の単位数(講義編集フォームで設定した単位数)を合計する。
- 設定した必要単位数に対する現在の取得単位数の進捗を、進捗バー等で視覚的に表示する。
- 学期をまたいだ累計で集計すること(過去学期の取得単位も合算対象とする)。

---

## 5. 通知機能

以下の4種類の通知を実装する。

| 通知種別 | タイミング | 内容 |
|---|---|---|
| 朝の時間割通知 | 毎朝7:00固定 | 「本日の時間割」として、当日の講義一覧を通知 |
| 欠席上限警告 | 欠席数が上限に近づいた時点(閾値は4.5参照、要確認) | 対象講義名と現在の欠席数/上限数を通知 |
| 課題締切通知 | スケジュール登録時に指定した通知タイミング(1日前/3日前) | 対象の課題タイトルと締切日時を通知 |
| (実装しないもの) | 1週間前通知 | ユーザー要望により対象外 |

**技術的制約(実装前提として明記):**

- 本アプリはPWAとして、iPhoneの「ホーム画面に追加」された状態でのみ通知が機能する(Safariでブラウザとして開いているだけでは通知は届かない、iOS 16.4以降の制約)。この制約はユーザーに既に説明済みであり、追加の確認は不要。
- Service Worker + Web Push APIの構成で実装すること。プッシュ通知サーバー(バックエンド)を新たに用意することは、完全無料方針・単一端末前提のため不要。ローカル通知(Notification API + Service Workerのスケジューリング)で実現可能な範囲で実装し、技術的に困難な場合はユーザーに選択肢を提示すること。

---

## 6. 画像エクスポート機能

- 週間時間割表全体(現在表示中の学期・週)を1枚の画像(PNG想定)として書き出し、端末に保存できる機能を実装する。
- 1日単位の切り出し機能は不要。
- 実装方法の一例として `html2canvas` 等のライブラリ利用が考えられるが、無料で利用できるライブラリであることを確認した上で採用すること。

---

## 7. データモデル(IndexedDB スキーマ設計方針)

以下はストア(テーブル相当)の設計方針である。フィールド名・型はClaude Codeの実装しやすい形に調整してよいが、保持すべき情報の範囲はこれに従うこと。

### 7.1 `semesters`(学期)

- id, 学期名(「前期」または「後期」固定), 年度, 開始日, 終了日, アクティブフラグ(現在表示中の学期かどうか)

### 7.2 `courses`(講義マスタ)

- id, 学期ID(4.9で要確認だが、暫定として学期に紐づける設計とする), 講義名, 担当教員名, 教室名, 単位数, 科目区分, シラバスURL, カラー(HEX), 出席管理対象フラグ, 欠席上限回数, 単位取得済みフラグ

### 7.3 `timetable_slots`(時間割配置)

- id, 学期ID, 曜日, 時限番号, 講義ID(配列またはリレーションで複数講義対応、4.7参照)

### 7.4 `attendance_records`(出欠記録)

- id, 講義ID, 日付, 種別(出席/欠席/遅刻/早退)

### 7.5 `schedules`(課題・スケジュール)

- id, 関連講義ID(nullable)、締切日時, カテゴリー(課題/レポート/テスト/休講/補講)、メモ, 完了フラグ, 通知タイミング(1日前/3日前/なし)

### 7.6 `period_settings`(時限設定)

- 時限番号, 開始時刻, 終了時刻

### 7.7 `display_settings`(表示設定)

- 表示対象曜日(曜日ごとのON/OFF)、必要単位数(進級/卒業要件)

### 7.8 バックアップ用エクスポート形式

- 上記全ストアの内容を1つのJSONファイルにまとめてエクスポートする。インポート時は既存データを全置換するか、マージするかをユーザーに確認するダイアログを実装すること。

---

## 8. 非機能要件

1. **完全無料であること。** 有料API・有料クラウドサービスは一切使用しない。
2. **オフライン動作すること。** ネットワーク接続がない状態でも、時間割の閲覧・登録・編集がすべて可能であること(Service Workerによるキャッシュ含む)。
3. **レスポンシブではなくモバイルファースト。** iPhoneでの表示を最優先とする。PC等での表示崩れは許容する(必須要件ではない)。
4. **データ消失リスクへの配慮。** IndexedDBのデータがブラウザ仕様で消去されるリスクをユーザーに開発初期段階で改めて説明し、エクスポート機能(7.8)の利用を促すUI(例:一定期間ごとにバックアップを促すリマインド)の実装を検討すること(必須ではないが推奨)。

---

## 9. 未確定事項・要確認リスト(実装開始前に必ずユーザーに確認すること)

本仕様書作成時点での未確定事項はすべて解消済みである。ただし、実装を進める中で本仕様書に記載のない挙動や曖昧な点が新たに見つかった場合は、0章の方針通り、推測で進めずユーザーに質問すること。

---

## 10. スコープ外(今回は実装しない)

- iOSネイティブアプリ化(App Store配信)
- 複数ユーザー対応・ログイン機能
- クラウド同期・複数端末同期
- クォーター制(4学期制)への対応
- 1日単位の時間割画像切り出し

---

## 11. 実装フェーズ(この順序で進めること)

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 0 | プロジェクト初期化 | Vite + React + Tailwindのセットアップ完了、ローカルサーバーで空白画面が表示できる |
| 1 | データ層構築 | 7章のIndexedDBスキーマを実装し、CRUD操作の動作確認ができる |
| 2 | 画面遷移の骨組み | 4タブ+主要画面への遷移が、ダミーデータで一通り動作する |
| 3 | 講義リスト・講義編集フォーム | 講義のCRUDがUI上から完結する |
| 4 | 時間割画面(コア機能) | コマへの配置・表示・タップ遷移・カラー表示が動作する |
| 5 | 授業詳細画面・出欠管理 | 出欠記録の登録・カウント表示・警告表示が動作する |
| 6 | スケジュール(課題管理) | 課題の登録・一覧表示・完了チェックが動作する |
| 7 | 学期切替・単位集計 | 学期の切替・過去データ閲覧・単位進捗表示が動作する |
| 8 | 設定機能 | 時限数・時刻・表示曜日のカスタマイズが動作する |
| 9 | PWA化・通知機能 | ホーム画面追加、Service Worker、各種通知が動作する |
| 10 | 画像エクスポート・バックアップ | 時間割の画像保存、JSONエクスポート/インポートが動作する |
| 11 | 実機確認・デプロイ | iPhone実機での動作確認、GitHub Pages等への公開 |

各フェーズ完了時点で、Claude Codeはユーザーに動作確認を依頼し、承認を得てから次フェーズに着手すること。
