# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際の運用ルールです。
**機能仕様の正典は [spec.md](spec.md)。** 実装内容の判断は必ず spec.md を参照すること。
**視覚デザインの正典は [ui-design-spec.md](ui-design-spec.md)。** 配色・フォント・装飾はこちらに従う(機能仕様は変更しない)。
本ファイルには spec.md の内容を複製せず、「守るべきルール」「現在の状態」「コマンド」のみを記載する。

---

## プロジェクト概要

大学の週間時間割・出欠・課題・単位取得状況を一元管理する **PWA**。

- 想定ユーザー: 開発者本人1名(大学1年生)。マルチユーザー機能は不要だが、**本人確認のためGoogleアカウントでのログインを導入する**(2026-09-07 方針変更)。
- 想定端末: **iPhone Safari(ホーム画面追加)+ PC(Windows/Mac)ブラウザ**。モバイルファーストの見た目は維持しつつ、PCでも実用できることを必須とする。
- **複数端末間でリアルタイムに同期する。** iPhoneで入力した内容が、PCでも(かつその逆も)速やかに反映されること。

---

## 絶対制約(逸脱不可)

1. **完全無料。** 有料API・有料クラウドサービス・従量課金が発生し得る構成は一切採用しない。
   無料枠のあるサービスでも、課金に転じ得るものは採用前に必ずユーザーに確認する。
2. **自前のバックエンドサーバーは立てない。** サーバーサイドのコードは書かない。Firestoreへのアクセスはすべてクライアント(ブラウザ)のSDKから直接行う(マネージドなBaaSの利用は本項に抵触しない)。
3. **クラウド同期には Firebase(Authentication + Cloud Firestore)の無料プラン(Sparkプラン)のみを使用する。**(2026-09-07 方針変更。旧: 「Firebase等は使用しない」)
   - **Blazeプラン(従量課金)への切り替えは絶対に行わない。** Sparkプランはクレジットカード登録不要で、性質上そのままでは課金が発生しない
   - Firestore の無料枠(1日あたり 読み取り5万・書き込み2万・削除2万、保存1GiB)は単一ユーザーの用途では通常到達しない想定。もし枠に近づく兆候が見えたら実装を止めてユーザーに相談する
4. **オフラインで完全動作すること。** 時間割の閲覧・登録・編集がネットワークなしで可能であること。Firestoreのオフライン永続化機能を使い、この要件を満たす。
5. 新しい npm パッケージを導入する際は、**ライセンスと無料性を確認**した上で、導入理由をユーザーに説明してから入れる。

---

## 技術スタック(確定・変更する場合は要相談)

| レイヤー | 採用技術 |
|---|---|
| フレームワーク | React(関数コンポーネント + Hooks) |
| スタイリング | Tailwind CSS(テーマ定義は src/index.css の @theme) |
| ビルド | Vite |
| PWA | vite-plugin-pwa(Service Worker / manifest 自動生成) |
| 認証 | Firebase Authentication(Googleログイン) |
| 永続化・同期 | Cloud Firestore(Sparkプラン限定、オフライン永続化を有効化) |
| バックアップ | JSON エクスポート/インポートを自前実装(Firestore移行後も現行データの避難用に維持) |
| アイコン | lucide-react(ISC) |
| テーマ | ダーク/ライトの2構成。CSS変数を `<html data-theme>` で切替 |
| レイアウト | 768px未満=スマホ幅+下部タブバー / 768px以上=左サイドバー+広い時間割 |
| フォント | Rajdhani / Share Tech Mono(SIL OFL、public/fonts に同梱) |
| 通知 | Service Worker + Notification API(ローカル通知ベース) |
| ホスティング | GitHub Pages または Cloudflare Pages(無料枠) |

---

## 作業の進め方(厳守)

- **返答は常に日本語。**
- **ユーザーはプログラミング初学者。** 技術的な選択を提案するときは、専門用語をかみ砕き、なぜそれを選ぶのかの理由を添えて説明する。
- **推測で実装しない。** spec.md に記載がない挙動、曖昧・矛盾する記述に出会ったら、実装を止めてユーザーに質問する。確認コスト < 手戻りコスト。
- **フェーズ単位で進める。** spec.md「11. 実装フェーズ」の順序を守る。1フェーズ完了ごとに動作確認を依頼し、**ユーザーの承認を得てから次フェーズに着手する**。フェーズを勝手に先取りしない。
- **変更は小さい単位で。** 大規模な一括変更・広範囲のリファクタは避ける。
- **ファイル/フォルダを削除しない。** 削除が必要な場合はプロジェクト直下に `削除用フォルダ/` を作成してそこへ移動し、実際に削除してよいかユーザーに確認する。
- コミット・プッシュはユーザーから指示があったときのみ行う。

---

## 実装上の注意

- **データ消失リスク**: IndexedDB はブラウザ都合で消える可能性がある。JSONエクスポートによるバックアップ導線を軽視しない。
- **設定変更でデータを壊さない**: 時限数を減らす・曜日を非表示にする等の設定変更で、既存の講義データを削除してはならない(「非表示だが保持」+ 警告表示)。
- **学期はデータの分離単位**: 講義マスタも時間割配置も学期ごとに独立。過去学期のデータは削除せず参照可能に保つ。単位集計のみ学期をまたいで累計する。
- **iOS 通知制約**: ホーム画面に追加された PWA でのみ通知が動作する(iOS 16.4+)。この制約はユーザー了承済みなので再説明不要。ローカル通知で実現困難な要件が出たら、選択肢を提示して判断を仰ぐ。
- **1コマ = 1講義**。複数講義の同時配置は実装しない。IndexedDBの`unique`インデックスに頼っていた重複防止は、Firestore移行後はトランザクションで自前実装する。
- **Firebase設定値(apiKey等)は秘匿情報ではない。** Googleの仕様上クライアントに公開される前提の値であり、公開リポジトリに含めてよい。アクセス制御はFirestoreセキュリティルールとAuthenticationで行う(値を隠すことがセキュリティ対策にはならない)。
- **セキュリティルールを書くまでは本番運用しない。** 「自分のuid配下のデータしか読み書きできない」ルールが有効になるまで、実データはFirestoreに置かない。

---

## コマンド

```bash
npm install       # 依存インストール
npm run dev       # 開発サーバー起動
npm run build     # 本番ビルド
npm run preview   # ビルド結果のローカル確認(PWA動作確認はこちら)
```

---

## 現在の状態

- [x] 仕様書作成(spec.md)
- [x] フェーズ0: プロジェクト初期化(Vite 8 + React 19 + Tailwind CSS 4)
- [x] フェーズ1: データ層構築(IndexedDB / idb)
- [x] フェーズ2: 画面遷移の骨組み(自前ナビゲーション)
- [x] UIデザイン適用(ui-design-spec.md / ダーク=SF調・ライト=白基調の2テーマ + 切替)
- [x] フェーズ3: 講義リスト・講義編集フォーム
- [x] フェーズ4: 時間割画面(コマへの配置・今日/現在時限のハイライト)
- [x] フェーズ5: 授業詳細画面・出欠管理
- [x] フェーズ6: スケジュール(課題管理)
- [x] フェーズ7: 学期切替・単位集計
- [x] フェーズ8: 設定機能(時限・曜日)
- [x] フェーズ9: PWA化・通知機能(オフライン動作・ホーム画面追加・3種の通知)
- [x] フェーズ10: 画像エクスポート(canvas自前描画)・JSONバックアップ
- [x] フェーズ11: デプロイ準備(相対パス化・GitHub Actions・README)
- [x] 出欠記録の削除UI改善(記録一覧をデフォルト展開に変更)

### Firestore移行(2026-09-07 着手)

- [x] フェーズ12: Firebase基盤構築(プロジェクト作成・Google Sign-In画面・認証状態管理)
      → Googleアカウントでのサインイン・サインアウトを実機相当の環境で確認済み
      → 【重要な知見】redirect方式はChromeの「バウンストラッキング対策」機能により、
        中継地点(Firebaseの認証ドメイン)の状態が消されサインインが完了しないことがある。
        通常のブラウザではpopup方式、PWA(ホーム画面起動)時のみredirect方式に自動で切替。
- [x] フェーズ13: データ層のFirestore移行 + セキュリティルール(実機での動作確認済み)
      - スキーマ: `users/{uid}/courses`, `users/{uid}/timetableSlots` など、
        既存IndexedDBのストア名をそのまま `users/{uid}/` 配下に置く
      - ルール: 「自分のuid配下のみ読み書き可」の1本(`firestore.rules` に控えあり)
      - 【設計変更】「1コマ1講義」(spec 4.7)と「講義・日付の重複防止」(spec 4.4)は、
        トランザクションではなく、ドキュメントIDを `学期ID_曜日_時限` /
        `講義ID_日付` の決定的な値にすることで実現(排他制御が不要になる)
      - db/以下をIndexedDB→Firestoreに全面書き換え(database.jsは旧データとして
        フェーズ15の移行導線のために温存、削除しない)
      - サインイン必須化。App.jsxにAuthGateを追加し、未サインイン時はSignInScreenのみ表示
      - 【副作用】ThemeProvider(ダーク/ライト)はサインイン前に動く必要があるため、
        Firestore同期をやめてlocalStorageのみに変更(=端末ごとに別テーマを選べる)
      - 開発用の firebase-tools(ローカルFirestoreエミュレータ)導入を検討したがJava要件のため撤回。
        引き続き実際の無料Firestoreプロジェクトに対して手動で動作確認する方針
- [ ] フェーズ14: リアルタイム同期・オフライン対応(onSnapshot購読 + オフライン永続化)
- [x] フェーズ15: 既存データの移行導線 → **専用の機能は不要と判明(2026-09-07 ユーザー指摘)**。
      既存の「バックアップ」画面(旧IndexedDB版で書き出し→新Firestore版で取り込み)を
      そのまま使えばよい。backup.js の importAll は旧形式の乱数idを新しい決定的id
      (`学期ID_曜日_時限` 等)に自動で組み替えるため、旧アプリでエクスポートした
      JSONをそのまま「全置換」でインポートすれば移行が完了する設計にしてある。
      移行時に見つけたバグ: 組み替えたドキュメントIDと、中身の`id`フィールドが
      食い違う不具合があったため修正済み(withCorrectId関数)。実地での動作確認は未実施
- [~] フェーズ16: 公開設定の更新(コード側は完了。GitHub Actionsに `VITE_FIREBASE_*` を
      Variablesとして渡すよう `deploy.yml` を修正、READMEに手順を追記。
      残りはユーザー側の手動作業: ①GitHubリポジトリのVariablesに6値を登録
      ②Firebase Console → Authentication → 承認済みドメイン に公開URLのドメインを追加)

進捗が動いたら、このチェックリストを更新すること。
