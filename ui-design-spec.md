# UIデザイン仕様追加(spec.md 補足)

## 0. 位置づけ

本ドキュメントは `spec.md` の補足資料であり、機能仕様には影響を与えない。UIの視覚的デザイン(配色・フォント・装飾)についてのみ規定する。

`spec.md` に記載された機能要件・画面構成・データモデルは、本ドキュメントの内容によって一切変更されない。本ドキュメントは「同じ機能を、どういう見た目で実装するか」を定めるものである。

すでに実装済みのフェーズ(フェーズ0〜2)がある場合、Claude Codeはまず現在の実装への影響範囲(主にCSS・Tailwindクラス・カラー定義)を確認し、機能ロジックに影響を与えない範囲でスタイルの適用・修正を行うこと。既存の動作を壊さないことを最優先とする。

---

## 1. デザインコンセプト

近未来的なSFインターフェースを基調とする。具体的には以下のようなモチーフを想起させるスタイルとする。

- 戦闘機・宇宙船のコックピット計器パネル
- HUD(ヘッドアップディスプレイ)表示
- サイバーパンク作品に登場する管理システム・端末画面

「時間割管理アプリ」という実用ツールでありながら、まるで管制システムのコンソールを操作しているかのような没入感・高揚感(ロマン)を演出することが目的である。

---

## 2. カラーパレット

### 2.1 基調色(ダークモード)

- 背景色は黒〜濃紺を基調とする。単色の黒(`#000000`)ではなく、わずかに青みがかった濃紺(例:`#0A0E1A` 〜 `#0D1117` 相当)を推奨する。
- パネル・カード等の要素は、背景色よりわずかに明るい濃紺〜ダークグレー(例:`#111827` 〜 `#1A1F2E` 相当)とし、背景との階層差を作る。

### 2.2 アクセントカラー(ネオン系)

以下のネオンカラーを、強調・区切り・状態表示に使用する。

| カラー | 用途の例 |
|---|---|
| シアン(電光青緑、例:`#00F0FF` 相当) | 主要なアクセント、選択中の状態、枠線の発光表現 |
| エレクトリックブルー(例:`#2D5FFF` 相当) | ボタン、リンク、アクティブなタブ |
| パープル(例:`#B24BFF` 相当) | サブアクセント、特定のカテゴリー識別 |
| アラートレッド(例:`#FF3B5C` 相当) | 欠席上限到達等の警告表示専用(4.5の赤色警告と対応させる) |

**重要な制約:** 講義ごとのカラー設定(spec.md 4.6)は、ユーザーが自由にHEX値を選べる仕様のまま維持する。上記のネオンパレットは、UIの「枠組み」(背景・ボタン・ナビゲーション等)に適用するものであり、ユーザーが設定する講義カラーとは独立した領域として扱うこと。

### 2.3 コントラスト・可読性の確保(最優先事項)

- 本文テキストは白、または非常に薄いグレー(例:`#E5E7EB` 相当)とし、背景とのコントラスト比はWCAG AA基準(通常テキストで4.5:1以上)を満たすこと。
- ネオンカラーを文字色として直接使用する場合は、必ず十分な太さ・サイズを確保し、小さい文字(教室名バッジ等)には使用を避け、背景色や枠線・アイコンでの表現に留めること。
- 時間割のコマ内に表示される「講義名」「教室名」など、実用上必ず読めなければならない情報は、装飾よりも可読性を優先すること。装飾効果(発光表現等)によって文字が視認しにくくなる場合は、装飾を弱めること。

---

## 3. UI要素・装飾表現

### 3.1 レイアウト

- パネル・カードの区切りは、直線的・幾何学的な線で構成する。角丸は最小限(0〜4px程度)に抑え、シャープな印象を優先する。これはspec.md作成時に検討した「柔らかい角丸」の方向性とは逆であり、本デザインコンセプトを優先する。
- 時間割グリッドの罫線は、通常のグレーではなく、わずかに発光したような細いシアン系の線(または半透明のネオンカラー)を用いることを検討する。

### 3.2 発光・グロー表現

- ボタンやアクティブな要素の枠線に、CSSの `box-shadow` を用いた淡い発光効果(グロー)を用いてよい。ただし多用すると視認性を損なうため、以下に限定すること。
  - 現在選択中のタブ・ボタン
  - 今日の曜日・現在時限のハイライト(spec.md 4.1のハイライト表示は、このグロー表現で実現することを推奨)
  - 警告表示(欠席上限到達時)

### 3.3 アイコン・インジケーター

- アイコンは、丸みのある可愛らしいスタイルではなく、線が細く直線的な(アウトライン系)アイコンセットを使用する。`lucide-react` 等、無料で利用できるアイコンライブラリの使用を推奨する(コストが発生しないことを条件とする)。
- 出欠管理カードの数値表示等は、デジタル表示・計器盤のような見せ方(等幅フォントの活用、区切り線の追加等)を検討してよい。

### 3.4 背景装飾(任意)

- 背景に、薄いグリッド線や、わずかなノイズ・グラデーションを敷くことで奥行きを演出してもよい。ただし、これによりページの読み込み・描画パフォーマンスが悪化しないよう、軽量な実装(CSSのみ、画像を使わない等)に限定すること。

---

## 4. フォント

- 見出し・数値表示等、雰囲気を演出したい箇所には、角ばった直線的な書体(例:Google Fontsの `Orbitron`、`Rajdhani`、`Share Tech Mono` 等、SF・近未来感のある無料フォント)の採用を検討する。
- 本文・長文(メモ欄の入力内容等)には、上記の装飾フォントを使わず、標準的な読みやすいフォント(システムフォント、または `Noto Sans JP` 等)を使用し、可読性を優先すること。
- 日本語表示が必須のため、採用するフォントが日本語(ひらがな・カタカナ・漢字)に対応しているか、対応していない場合は日本語部分にフォールバックフォントを設定することを確認すること。

---

## 5. 実装上の注意点(Claude Codeへ)

1. **機能を壊さないこと。** 本ドキュメントはあくまでスタイル(見た目)の指示であり、spec.mdに定めた機能・データ構造・画面遷移には一切影響を与えないこと。
2. **可読性を最優先すること。** デザイン性と実用性が競合する場面(例:発光効果で文字が読みにくくなる等)では、必ず実用性(可読性)を優先し、判断に迷う場合はユーザーに確認すること。
3. **段階的に適用すること。** 一度に全画面のスタイルを変更するのではなく、Tailwindのテーマ設定(カラー変数等)から着手し、共通コンポーネント単位で適用していくことを推奨する。これにより、後から色味の調整が必要になった際の手戻りを最小化できる。
4. **既存フェーズへの遡及適用が必要な場合は、その旨をユーザーに報告すること。** 特にフェーズ0〜2で作成済みの画面がある場合、本デザイン方針の適用にどの程度の手戻りが発生するかを見積もり、報告した上で作業を進めること。
